## 1. Entre Semana

- [x] 1.1 En `ParteRow.tsx`, condicionar el badge "Pendiente" con `canEdit`: solo renderizarlo cuando `canEdit === true`

## 2. Fin de Semana

- [x] 2.1 En `ProgramaFDSView.tsx`, condicionar el badge "Pendiente" con `canEdit`: solo renderizarlo cuando `canEdit === true`
